# Database credentials
POSTGRES_USER=todos_user
POSTGRES_PASSWORD=todos_password
POSTGRES_DB=todos

# Database connection parameters
DB_HOST=localhost
DB_PORT=5432

# Database URL (автоматически использует значения выше)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}

# Optional: Server configuration (раскомментируйте для продакшена)
# HOST=0.0.0.0
# PORT=8000
# CORS_ORIGINS=http://localhost:8080,http://your-domain.com 